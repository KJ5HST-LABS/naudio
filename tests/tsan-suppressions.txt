# ThreadSanitizer suppressions for the naudio suite (issue #28).
#
# RULES FOR THIS FILE, so it never becomes the dumping ground the issue warned about:
#
#   1. One entry per line, each preceded by the issue number it defers and the failure it hides.
#   2. An entry is a MARKER FOR OPEN WORK, never an acceptance. Deleting it must reproduce a real,
#      filed defect — if deleting a line changes nothing, the line is stale and goes.
#   3. Third-party noise (PortAudio / ALSA internals) would be the other legitimate category. There
#      is none today: the first instrumented run reported no race outside naudio's own code.
#
# Nothing here is suppressed because it was hard to read. Every line costs an issue.

# ---------------------------------------------------------------------------------------------
# THERE ARE NO SUPPRESSIONS. The file stays because the job passes its path in TSAN_OPTIONS
# unconditionally and TSan refuses to start if it cannot read it — and because this is where the
# next one goes, under rule 1 above.
#
# The only entry this file ever held was `race:closeNative`, for issue #90: Socket::close() freeing
# a descriptor while a worker thread was blocked in ::recv() on it, 17 arms in the first
# instrumented run. It was removed when #90 was fixed rather than because it had grown quiet —
# Socket::close() now takes the handle, ::shutdown()s it, waits for every in-flight syscall to
# leave, and only then frees the number (include/naudio/net/Socket.hpp, Socket::close). Rule 2 is
# what retired it: deleting the line has to reproduce a filed defect, and after the fix it does not.
